# Copy this file to .env when running the Jira MCP server outside Codex.
# Do not commit .env.

# Used when JIRA_CONFIG is not set, or to override values from the config file.
JIRA_SERVER=https://your-domain.atlassian.net
JIRA_EMAIL=your.email@example.com
JIRA_API_TOKEN=your-atlassian-api-token
