{
  "name": "devteam-dashboard",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.15.0"
  },
  "workspaces": [
    "shared",
    "orchestration",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n app,server,client,inngest,worker -c blue,green,cyan,yellow,magenta \"npm run dev:appserver\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:inngest\" \"npm run dev:worker\"",
    "dev:appserver": "node scripts/start-appserver.js",
    "dev:server": "npm run dev --host=0.0.0.0 --workspace=server",
    "dev:client": "npm run dev --host=0.0.0.0 --workspace=client",
    "dev:inngest": "docker compose -f ../docker-compose.inngest.yml up",
    "dev:worker": "npm run dev:worker --workspace=orchestration",
    "build": "npm run build --workspace=shared && npm run build --workspace=orchestration && npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "start:worker": "npm run start:worker --workspace=orchestration",
    "flow": "npm run flow --workspace=orchestration --"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
